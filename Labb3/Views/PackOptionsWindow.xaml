<Window x:Class="Labb3.Views.PackOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pack Options" Height="300" Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">


    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

     
        <Label Content="Pack Name:" FontWeight="Bold" Grid.Row="0"/>
        <TextBox Text="{Binding PackName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,15" Grid.Row="1" Height="25"/>

     
        
        <Label Content="Difficulty:" FontWeight="Bold" Grid.Row="2"/>
        <ComboBox SelectedIndex="{Binding SelectedDifficultyIndex, UpdateSourceTrigger=PropertyChanged}" 
              Margin="0,5,0,15" Grid.Row="3" Height="25">
            <ComboBoxItem Content="Easy"/>
            <ComboBoxItem Content="Medium"/>
            <ComboBoxItem Content="Hard"/>
        </ComboBox>
        

        <Label Content="Time Limit:" FontWeight="Bold" Grid.Row="4"/>
        <StackPanel Margin="0,5,0,0" Grid.Row="5">
            <Slider Minimum="5" Maximum="120" Value="{Binding TimeLimit, UpdateSourceTrigger=PropertyChanged}" 
                TickFrequency="5" IsSnapToTickEnabled="True" 
                TickPlacement="BottomRight" AutoToolTipPlacement="TopLeft"/>
            <TextBlock Text="{Binding TimeLimit, StringFormat='{}{0} seconds'}" 
                   HorizontalAlignment="Center" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>
        
    </Grid>
    
    
</Window>