<UserControl x:Class="Labb3.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

     
        <DockPanel Grid.Row="0" Grid.Column="0">
            <Label Content="{Binding ActivePack.Name}" FontWeight="Bold" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Margin="4" Content="Åtgärder" Command="{Binding OpenPackOptionsCommand}"/>
                <Button Margin="4" Content="+" Command="{Binding AddQuestionCommand}"/>
                <Button Margin="4" Content="-" Command="{Binding RemoveQuestionCommand}"/>
            </StackPanel>
        </DockPanel>

        <ListBox Grid.Row="1" Grid.Column="0" Margin="5" ItemsSource="{Binding ActivePack.Questions}" 
                 SelectedItem="{Binding ActiveQuestion}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="Question:" FontWeight="Bold"/>
                        <TextBlock Text="{Binding Query}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Column="1" Grid.RowSpan="2" Margin="10">
            <Label Content="Question:" FontWeight="Bold" />
            <TextBox Height="60" Text="{Binding ActiveQuestion.Query}" Margin="0,0,0,10" />

            <Label Content="Correct Answer:" FontWeight="Bold"/>
            <TextBox Text="{Binding ActiveQuestion.CorrectAnswer}" Margin="0,0,0,10" />

            <Label Content="Incorrect Answers:" FontWeight="Bold"/>
            <TextBox Text="{Binding ActiveQuestion.IncorrectAnswers[0]}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ActiveQuestion.IncorrectAnswers[1]}" Margin="0,0,0,5"/>
            <TextBox Text="{Binding ActiveQuestion.IncorrectAnswers[2]}" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,10,0,0">
                <ComboBox Width="120" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="Name"/>
                <Button Content="+" Command="{Binding AddCategoryCommand}" Margin="5,0"/>
                <Button Content="-" Command="{Binding RemoveCategoryCommand}" Margin="5,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

